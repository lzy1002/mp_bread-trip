<!--pages/place/place.wxml-->
<my-place-header placeHeaderData="{{placeHeaderData}}"></my-place-header>

<my-place-hot hotData="{{hotData}}" type="{{type}}" placeId="{{placeId}}"></my-place-hot>

<my-loading wx:if="{{hotData.length <= 0}}"></my-loading>
